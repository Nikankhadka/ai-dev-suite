# Plan: Global Agent Development Suite

## Pre-Step: Commit Current Baseline
- Stage all modified + untracked files in `~/.config/opencode/`
- Commit as "chore: snapshot ECC simplification before global agent suite setup"
- Purpose: easy `git revert` if anything goes wrong

## Phase 1: Memory & Cross-Harness Bridge

### 1.1 — Create `~/.claude/CLAUDE.md`
Content: the 7 global rules (no em-dash, no auto co-author, no manual CHANGELOG edits, prefer quality over dev cost, E2E-first bug repro, pixel-perfection, fix lint/test failures on sight).

### 1.2 — Bridge into OpenCode
Edit `opencode.jsonc` instructions array:
```jsonc
"instructions": [
  "instructions/INSTRUCTIONS.md",
  "~/.claude/CLAUDE.md"
]
```
Absolute path — confirmed working per OpenCode schema. No symlink.

### 1.3 — Bridge into Codex CLI
```bash
ln -sf ~/.claude/CLAUDE.md ~/.codex/AGENTS.md
```
Codex's global file is `AGENTS.md`, not `CLAUDE.md`. Currently exists (0 bytes).

### 1.4 — Trim INSTRUCTIONS.md
Add one line at the top: "> Global cross-harness rules live in `~/.claude/CLAUDE.md`. This file is OpenCode-specific."
Keep agent table, command table, guardrails as-is.

### 1.5 — Project-level template
Create `~/.config/opencode/AGENTS.md.template` with sections: project context, repo layout, terminology, conventions, E2E notes, architecture overview.

**Verify:** `readlink ~/.codex/AGENTS.md` resolves; `opencode.jsonc` parses valid JSON.

## Phase 2: Install Kunchen's Tool Suite

All tools: `git clone` into `~/.local/share/<tool>`, READ the fetched installer before running.

### 2.0 — Prerequisites
- `gh auth login` (gh-axi depends on authenticated gh)
- `brew install go` (treehouse needs `go install`)

### 2.1 — npm-installable tools (inspect package.json first, then `npm install -g`)
| Tool | Install |
|------|---------|
| gh-axi | `npm install -g gh-axi` |
| chrome-devtools-axi | `npm install -g chrome-devtools-axi` |
| gnhf | `npm install -g gnhf`; config `~/.gnhf/config.yml` |
| quota-axi | `npm install -g quota-axi` |

### 2.2 — no-mistakes
Clone → read `docs/install.sh` → run it → `no-mistakes init` to configure local gate.

### 2.3 — lavish-axi
`npx skills add kunchenguid/lavish-axi --skill lavish`

### 2.4 — treehouse
`go install github.com/kunchenguid/treehouse@latest` (install.sh returned 404 on main; Go brew-installed in 2.0).

### 2.5 — firstmate
Clone → inspect → `cd firstmate && claude` per bootstrap docs. tmux 3.6b is already installed.

**Verify:** `gh-axi --help`, `gnhf --help`, `quota-axi --help`, `treehouse --help`, `no-mistakes --help` all return non-error.

## Phase 3: OpenCode Integration

### 3.0 — gh-axi first, gh_grep MCP as fallback
Keep `gh_grep` MCP entry in `opencode.jsonc` but document in INSTRUCTIONS.md that `gh-axi` is the primary tool (lower cost, no round-trip). Only fall back to MCP if axi fails.

### 3.1 — `/nomistakes` command
File: `commands/nomistakes.md` → wired to `ops` agent, triggers no-mistakes pipeline in current repo.

### 3.2 — lavish skill
Create `skills/lavish/SKILL.md` with description "Interactive HTML planning artifacts for complex feature design. Load when doing architectural planning or design discussions." Describe how to invoke `npx lavish-axi`.

### 3.3 — Update planner prompt
Edit `prompts/agents/planner.txt`: for complex/architectural plans, offer a lavish HTML artifact as an alternative to plain-text plans.

### 3.4 — `/gnhf` command
File: `commands/gnhf.md` → launches overnight autonomous loop with objective + `--max-iterations`/`--max-tokens`.

### 3.5 — `/treehouse` command
File: `commands/treehouse.md` → wraps `treehouse get`/`status`/`return` for pooled worktree sessions.

### 3.6 — `/firstmate` command
File: `commands/firstmate.md` → boots firstmate orchestrator in a treehouse worktree with a crew of sub-agents.

**Verify:** Each new command shows in OpenCode's slash-completion; `/nomistakes` on a scratch branch runs end-to-end.

## Phase 4: Voice Input

### 4.1 — Install Open Super Whisper
Download from GitHub releases, install local Whisper model for offline transcription.

### 4.2 — Custom vocabulary
Seed with project/tool names: axi, gnhf, treehouse, firstmate, no-mistakes, lavish, kunchen, CLAUDE.md, Codex CLI.

### 4.3 — Hotkey config
Configure a system hotkey, verify voice-to-text pipes correctly into terminal for at least one harness.

**Verify:** Dictate a sentence with 2-3 project terms and confirm correct transcription.

## Verification Checklist (end-to-end)
1. `cat ~/.claude/CLAUDE.md` shows the 7 global rules
2. `readlink ~/.codex/AGENTS.md` → `~/.claude/CLAUDE.md`
3. Fresh OpenCode session: `/system` or prompt confirms global rules loaded
4. All 8 tools respond to `--help`
5. `/nomistakes`, `/gnhf`, `/treehouse`, `/firstmate` are recognized slash commands
6. `gh-axi` works (requires `gh auth login` first)
7. Voice input transcribes correctly

## Files Modified
| File | Action |
|------|--------|
| `~/.claude/CLAUDE.md` | **Create** (new) |
| `~/.codex/AGENTS.md` | **Symlink** → `~/.claude/CLAUDE.md` |
| `opencode.jsonc` | **Edit** — add `~/.claude/CLAUDE.md` to instructions array; keep gh_grep MCP |
| `instructions/INSTRUCTIONS.md` | **Edit** — add one-line cross-harness note |
| `~/.config/opencode/AGENTS.md.template` | **Create** (new) |
| `commands/nomistakes.md` | **Create** (new) |
| `commands/gnhf.md` | **Create** (new) |
| `commands/treehouse.md` | **Create** (new) |
| `commands/firstmate.md` | **Create** (new) |
| `skills/lavish/SKILL.md` | **Create** (new) |
| `prompts/agents/planner.txt` | **Edit** — add lavish offer clause |
