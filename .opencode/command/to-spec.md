---
description: Synthesize the current conversation into a spec and publish it to the issue tracker
agent: planner
---

Turn the current conversation into a spec (PRD). Do NOT interview the user - just synthesize what you already know. Explore the codebase, sketch the testing seams, then write the spec using the template (Problem Statement, Solution, User Stories, Implementation Decisions, Testing Decisions, Out of Scope). Publish to the issue tracker with the `ready-for-agent` label.
