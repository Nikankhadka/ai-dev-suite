---
description: Build a throwaway prototype to answer a design question — logic/state model or UI variations
agent: builder
---

Run the prototype skill. Build a throwaway prototype to answer a design question. Determine which branch fits: logic/state model (tiny interactive terminal app) or UI (several radically different variations on one route). Rules: clearly marked as throwaway, one command to run, no persistence, skip polish, surface state after every action.
