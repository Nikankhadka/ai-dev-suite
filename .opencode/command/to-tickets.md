---
description: Break a plan, spec, or conversation into tracer-bullet tickets with blocking edges
agent: planner
---

Run the to-tickets skill. Take the current spec, plan, or conversation and break it into a set of tracer-bullet tickets. Each ticket must declare its blocking edges - what blocks it and what it blocks. Write tickets as text in a local file or as native blocking links on the issue tracker.
