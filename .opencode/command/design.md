---
description: Frontend design — invokes the design router to pick between Hallmark (structured HTML/CSS) and Taste-Skill (animated React/Next.js) based on project context
agent: planner
---
Run the frontend-design skill. Detect the project context (framework stack, animation needs, type of frontend task) and route to the correct design skill. Ask the user for input (brief, audience, tone) if the context isn't clear. Produce a design plan that the builder can execute.
