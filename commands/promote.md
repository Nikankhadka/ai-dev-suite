---
description: Promote project instincts to global scope
agent: build
---

# Promote Command

Promote instincts in continuous-learning-v2: $ARGUMENTS

## Your Task

Run:

```bash
python3 "${ECC_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" promote $ARGUMENTS
```

If `ECC_PLUGIN_ROOT` is unavailable, check `CLAUDE_PLUGIN_ROOT` for backward compatibility:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" promote $ARGUMENTS
```

