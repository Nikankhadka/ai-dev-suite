---
description: List registered projects and instinct counts
agent: build
---

# Projects Command

Show continuous-learning-v2 project registry and stats: $ARGUMENTS

## Your Task

Run:

```bash
python3 "${ECC_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" projects
```

If `ECC_PLUGIN_ROOT` is unavailable, check `CLAUDE_PLUGIN_ROOT` for backward compatibility:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" projects
```

