---
description: Autonomous long-running agent loop - give an objective and let it iterate until done
agent: builder
---

Run the gnhf skill ("good night, have fun"). Set up an autonomous agent orchestrator that repeatedly calls a coding agent until a stop condition is met. Each successful iteration is committed. Useful for overnight runs on verifiable objectives (reducing page load time, increasing test coverage, auto-fixing issues). Supports token caps, iteration caps, and stop conditions.
